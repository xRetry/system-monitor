syntax = "proto3";
package sysmon;

service SysMon {
    rpc CheckStatus () return (StatusResponse)
} 

message StatusResponse {
    string error_message = 1;
    uint32 uptime_sec = 2;
    uint32 running_progams = 3;
}
